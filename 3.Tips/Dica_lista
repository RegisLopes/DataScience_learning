
#Transformando itens de uma lista em float 

  x_i=[]
  a=0
  
  b=int(input("Tamanho da lista: "))
  #print(type(x),type(a),type(b))

  while a<b:
    c=int(input("Item: "))
    x_i.append(c)
    a+=1

  x_f=[float(i) for i in x_i]
    
    
  print("{%}----->{%}".format(x_i,x_f))
